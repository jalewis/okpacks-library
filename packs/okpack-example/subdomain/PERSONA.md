## Installed domain: example (okpack-example co-install)

The vault also carries okpack-example's types (topic / playbook). This is the marked
persona section `framework install-domain` appends to the deployment CLAUDE.md — ship
your pack's curation rules here: what the types mean, what evidence standard applies,
what the agent must never do with them. The HOST persona still governs voice, depth,
and mission priority.
