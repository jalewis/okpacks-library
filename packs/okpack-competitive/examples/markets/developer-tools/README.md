# Sample market: developer tools
SCM/CI platforms, IDEs, and app platforms — fast release cadence and an active AI-feature arms
race, so the signal volume is high and segment boundaries move (good stress test for
trend/segment lanes).
Setup: copy `feeds.opml` → `feeds/feeds.opml`, `watchlist.yaml` → `config/competitive-watchlist.yaml`, deploy.
