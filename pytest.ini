[pytest]
# Pack conformance suites intentionally reuse descriptive filenames such as
# test_pages.py. Importlib mode gives each path an isolated module identity so
# the whole library can be collected as one product in addition to the public
# CI's standalone-per-pack runner.
addopts = --import-mode=importlib
